{
  "name": "Yggdrasil via `localhost:8080`",
  "version": "1.0.0",
  "description": "Proxy via `localhost:8080` if the domain can't be resolved or if the resolved IP is not in Yggdrasils `0200::/7` network range.",
  "permissions": [
    "proxy"
  ],
  "background": {
    "scripts": ["background.js"],
    "persistent": false
  },
  "browser_action": {
    "default_icon": {
      "128": "y.128.png"
    }
  },
  "icons": {"128": "y-green.128.png"},
  "manifest_version": 2
}
